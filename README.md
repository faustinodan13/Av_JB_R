Primeiro commit para teste e iniciar a AV_JB_R
teste 2

#erro aqui#
envir.coord <- merge(X = envir,
                     Y = coord,
                     by = "Sites")
#próximo passo#

dim(envir)
dim(coord)
dim(envir.coord)
head(envir.coord)
